name: Deploy

on:
  push:
    branches: [main]

jobs:
  vercel:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Vercel deploy placeholder
        run: echo "Connect this repository to Vercel for production deployments."
